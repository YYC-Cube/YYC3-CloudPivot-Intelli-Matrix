# YYC³ CloudPivot Intelli-Matrix - 环境变量配置

## 📋 环境变量配置

### 配置文件结构

```
/Volumes/Max/YYC3-CloudPivot-Intelli-Matrix/
├── .env                    # 当前环境变量（开发/测试）
├── .env.example            # 环境变量模板（所有配置项）
├── .env.development        # 开发环境变量
├── .env.production         # 生产环境变量
├── .env.test              # 测试环境变量
└── .env.local             # 本地环境变量（不提交到 Git）
```

### 环境变量优先级

1. **开发环境** (`.env.development`)
   - 开发模式启用
   - 调试模式启用
   - 本地开发服务器

2. **测试环境** (`.env.test`)
   - 测试模式启用
   - 测试数据库
   - Mock 数据

3. **生产环境** (`.env.production`)
   - 生产模式启用
   - 生产数据库
   - 生产 API 地址

4. **本地环境** (`.env.local`)
   - 本地开发
   - 个人配置（不提交）

5. **当前环境** (`.env`)
   - 覆盖所有环境
   - 用于快速切换环境

---

## 🔧 环境变量分类

### 1. 应用配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `VITE_APP_NAME` | 应用名称 | YYC³ CloudPivot Intelli-Matrix | ❌ |
| `VITE_APP_VERSION` | 应用版本 | 0.0.1 | ❌ |
| `VITE_APP_URL` | 应用部署 URL | http://localhost:3118 | ❌ |
| `VITE_DEFAULT_THEME` | 默认主题 | cyberpunk | ❌ |
| `VITE_DEFAULT_LANGUAGE` | 默认语言 | zh-CN | ❌ |

### 2. 功能开关

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `VITE_ENABLE_GHOST_MODE` | 启用幽灵模式 | true | ❌ |
| `VITE_ENABLE_PWA` | 启用 PWA | true | ❌ |
| `VITE_ENABLE_AI_ASSISTANT` | 启用 AI 助手 | true | ❌ |
| `VITE_DEV_MODE` | 开发模式 | true | ❌ |
| `VITE_DEBUG_MODE` | 调试模式 | false | ❌ |

### 3. API 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `VITE_API_BASE_URL` | API 基础 URL | http://localhost:3118/api | ❌ |
| `API_PROXY_BASE_URL` | API 代理 URL | https://api.0379.world | ❌ |
| `API_PROXY_TIMEOUT` | API 代理超时（毫秒） | 30000 | ❌ |
| `API_PROXY_VERSION` | API 代理版本 | 1.0.0 | ❌ |
| `API_HOST` | API 主机地址 | 0.0.0.0 | ❌ |
| `API_PORT` | API 端口 | 8000 | ❌ |
| `API_WORKERS` | API 工作进程数 | 4 | ❌ |
| `API_DEBUG` | API 调试模式 | false | ❌ |
| `API_LOG_LEVEL` | API 日志级别 | info | ❌ |

### 4. WebSocket 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `VITE_WS_URL` | WebSocket 服务器地址 | ws://localhost:3113/ws | ❌ |
| `VITE_WS_RECONNECT_INTERVAL` | WebSocket 重连间隔（毫秒） | 5000 | ❌ |
| `VITE_WS_MAX_RECONNECT_ATTEMPTS` | WebSocket 最大重连次数 | 10 | ❌ |
| `VITE_WS_HEARTBEAT_INTERVAL` | WebSocket 心跳间隔（毫秒） | 30000 | ❌ |

### 5. 日志配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `VITE_LOG_LEVEL` | 日志级别（debug, info, warn, error） | info | ❌ |

### 6. 数据库配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `POSTGRES_PASSWORD` | PostgreSQL 密码 | your_postgres_password | ✅ |
| `DB_HOST` | 数据库主机地址 | postgres | ✅ |
| `DB_PORT` | 数据库端口 | 5432 | ✅ |
| `DB_USER` | 数据库用户名 | postgres | ✅ |
| `DB_PASSWORD` | 数据库密码 | your_postgres_password | ✅ |
| `DB_NAME` | 数据库名称 | yyc3_gpt | ✅ |

### 7. Redis 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `REDIS_HOST` | Redis 主机地址 | redis | ✅ |
| `REDIS_PORT` | Redis 端口 | 6379 | ✅ |
| `REDIS_DB` | Redis 数据库编号 | 0 | ✅ |
| `REDIS_PASSWORD` | Redis 密码 | (空) | ❌ |

### 8. AI 模型配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `OPENAI_API_KEY` | OpenAI API 密钥 | (空) | ❌ |
| `ZHIPU_API_KEY` | 智谱 AI API 密钥 | (空) | ❌ |
| `OLLAMA_HOST` | Ollama 主机地址 | host.docker.internal | ❌ |
| `OLLAMA_PORT` | Ollama 端口 | 11435 | ❌ |
| `OLLAMA_MODELS_PATH` | Ollama 模型存储路径 | /mnt/models | ❌ |

### 9. 网络配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `HOST_IP` | 主机 IP 地址 | 10.200.0.2 | ❌ |
| `HOST_IP_SUFFIX` | 主机 IP 后缀 | 2 | ❌ |

### 10. 监控配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `PROMETHEUS_MULTIPROC_DIR` | Prometheus 多进程目录 | /tmp/prometheus_multiproc | ❌ |
| `PROMETHEUS_ENABLED` | 启用 Prometheus | true | ❌ |
| `GRAFANA_ENABLED` | 启用 Grafana | true | ❌ |
| `ALERTMANAGER_ENABLED` | 启用 Alertmanager | true | ❌ |

### 11. MCP 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `BRAVE_API_KEY` | Brave Search API 密钥 | (空) | ❌ |
| `GITHUB_PERSONAL_ACCESS_TOKEN` | GitHub 个人访问令牌 | (空) | ❌ |

### 12. 域名配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `API_DOMAIN` | API 域名 | api.0379.world | ❌ |
| `GRAFANA_DOMAIN` | Grafana 域名 | grafana.0379.world | ❌ |
| `PROMETHEUS_DOMAIN` | Prometheus 域名 | prometheus.0379.world | ❌ |

### 13. 端口映射配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `POSTGRES_PORT_MAPPING` | PostgreSQL 端口映射 | 5432:5433 | ❌ |
| `REDIS_PORT_MAPPING` | Redis 端口映射 | 6379:6379 | ❌ |
| `OLLAMA_PORT_MAPPING` | Ollama 端口映射 | 11435:11435 | ❌ |
| `API_PORT_MAPPING` | API 端口映射 | 8000:8000 | ❌ |
| `GRAFANA_PORT_MAPPING` | Grafana 端口映射 | 3000:3000 | ❌ |
| `PROMETHEUS_PORT_MAPPING` | Prometheus 端口映射 | 9090:9090 | ❌ |
| `TRAEFIK_PORT_MAPPING` | Traefik 端口映射 | 80:80,443:443,8080:8080 | ❌ |

### 14. CloudPivot Matrix 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `CLOUDPIVOT_MATRIX_PORT` | CloudPivot Matrix 端口 | 3118 | ❌ |
| `CLOUDPIVOT_MATRIX_WS_PORT` | CloudPivot Matrix WebSocket 端口 | 3113 | ❌ |
| `CLOUDPIVOT_MATRIX_HOST` | CloudPivot Matrix 主机地址 | 192.168.3.22 | ❌ |

### 15. 缓存配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `CACHE_ENABLED` | 启用缓存 | true | ❌ |
| `CACHE_TTL` | 缓存过期时间（秒） | 3600 | ❌ |
| `CACHE_MAX_SIZE` | 缓存最大条目数 | 1000 | ❌ |

### 16. 速率限制配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `RATE_LIMIT_ENABLED` | 启用速率限制 | true | ❌ |
| `RATE_LIMIT_REQUESTS` | 速率限制请求数 | 100 | ❌ |
| `RATE_LIMIT_WINDOW` | 速率限制时间窗口（秒） | 60 | ❌ |

### 17. SSL/TLS 配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `SSL_ENABLED` | 启用 SSL | true | ❌ |
| `SSL_CERT_PATH` | SSL 证书路径 | /etc/ssl/certs/api.0379.world.crt | ❌ |
| `SSL_KEY_PATH` | SSL 私钥路径 | /etc/ssl/private/api.0379.world.key | ❌ |

### 18. 安全配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `CORS_ENABLED` | 启用 CORS | true | ❌ |
| `CORS_ORIGINS` | CORS 允许的源 | https://0379.world,https://api.0379.world | ❌ |
| `CORS_METHODS` | CORS 允许的方法 | GET,POST,PUT,DELETE,OPTIONS | ❌ |
| `CORS_HEADERS` | CORS 允许的头部 | Content-Type,Authorization | ❌ |
| `VITE_ENABLE_CSP` | 启用 CSP | true | ❌ |
| `VITE_ENABLE_XSS_PROTECTION` | 启用 XSS 保护 | true | ❌ |

### 19. 备份配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `BACKUP_ENABLED` | 启用备份 | true | ❌ |
| `BACKUP_SCHEDULE` | 备份计划（Cron 表达式） | 0 2 * * | ❌ |
| `BACKUP_RETENTION_DAYS` | 备份保留天数 | 30 | ❌ |
| `BACKUP_PATH` | 备份路径 | /Users/yanyu/nfs_vpn_mount/backup | ❌ |

### 20. NFS 挂载配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `NFS_HOST` | NFS 服务器主机地址 | 192.168.3.45 | ❌ |
| `NFS_PATH` | NFS 共享路径 | /Volume2/yyc3-33 | ❌ |
| `NFS_MOUNT_POINT` | 本地挂载点 | /Users/yanyu/nfs_vpn_mount | ❌ |

### 21. 健康检查配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `HEALTH_CHECK_ENABLED` | 启用健康检查 | true | ❌ |
| `HEALTH_CHECK_INTERVAL` | 健康检查间隔（秒） | 30 | ❌ |
| `HEALTH_CHECK_TIMEOUT` | 健康检查超时（秒） | 10 | ❌ |
| `HEALTH_CHECK_RETRIES` | 健康检查重试次数 | 3 | ❌ |

### 22. 开发工具配置

| 变量名 | 说明 | 默认值 | 必需 |
|---------|------|---------|------|
| `HOT_RELOAD` | 热重载 | true | ❌ |
| `AUTO_RESTART` | 自动重启 | true | ❌ |
| `DEBUG_MODE` | 调试模式 | false | ❌ |
| `VERBOSE_LOGGING` | 详细日志 | false | ❌ |
| `PROFILING_ENABLED` | 性能分析 | false | ❌ |

---

## 🎯 环境变量使用指南

### 开发环境

```bash
# 复制开发环境配置
cp .env.development .env

# 启动开发服务器
pnpm dev
```

### 生产环境

```bash
# 复制生产环境配置
cp .env.production .env

# 构建生产包
pnpm build

# 部署到生产服务器
# 使用 Docker 或直接部署
```

### 测试环境

```bash
# 复制测试环境配置
cp .env.test .env

# 运行测试
pnpm test
```

---

## 🔒 安全注意事项

### 1. 敏感信息保护

- ✅ `.env` 和 `.env.local` 已添加到 `.gitignore`
- ✅ 生产环境变量文件不提交到 Git
- ✅ API 密钥使用环境变量，不硬编码

### 2. 生产环境部署

- ✅ 使用 `.env.production` 配置
- ✅ 确保所有必需的环境变量都已设置
- ✅ 使用强密码和密钥
- ✅ 启用 SSL/TLS
- ✅ 配置 CORS 和 CSP

### 3. 开发环境配置

- ✅ 使用 `.env.development` 配置
- ✅ 启用调试模式
- ✅ 使用本地数据库和 Redis

---

## 📊 环境变量统计

| 类别 | 数量 |
|------|------|
| 应用配置 | 6 |
| 功能开关 | 5 |
| API 配置 | 10 |
| WebSocket 配置 | 4 |
| 日志配置 | 1 |
| 数据库配置 | 5 |
| Redis 配置 | 4 |
| AI 模型配置 | 5 |
| 网络配置 | 2 |
| 监控配置 | 4 |
| MCP 配置 | 2 |
| 域名配置 | 3 |
| 端口映射配置 | 6 |
| CloudPivot Matrix 配置 | 3 |
| 缓存配置 | 3 |
| 速率限制配置 | 3 |
| SSL/TLS 配置 | 3 |
| 安全配置 | 5 |
| 备份配置 | 4 |
| NFS 挂载配置 | 3 |
| 健康检查配置 | 4 |
| 开发工具配置 | 5 |
| **总计** | **88** |

---

## 🔗 相关文档

- [配置文件分析报告](./配置文件分析报告.md)
- [项目文档 README](./docs/README.md)
- [应用文档 README](./src/app/docs/README.md)

---

**最后更新**: 2026-03-26
**维护者**: YYC³ 团队
